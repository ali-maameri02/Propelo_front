server {
    listen 80;
    server_name propelo.cam www.propelo.cam;

    location / {
        root /usr/share/nginx/html;  # Path where the built files are served
        try_files $uri $uri/ /index.html;  # Fallback to index.html for SPA
    }

    location /static {
        alias /usr/share/nginx/html/static;  # Adjust if needed
    }
}
